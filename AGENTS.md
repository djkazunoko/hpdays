# AGENTS.md

hpdaysで作業するAIコーディングエージェント向けのガイドラインです。

## プロジェクト

hpdaysは、Hello! Projectメンバーの在籍日数を中心に、
メンバーの在籍記録やグループの歴史を時間軸から楽しむWebアプリです。

主な技術構成は以下です。

- React
- TypeScript
- Vite
- Vitest
- JSON
- Cloudflare Pages

仕様や設計については、コードだけから推測せず、
GitHub Issue・Pull Requestに記録されている内容を確認してください。

## GitHubの情報

GitHub CLI (`gh`) が利用できることを前提とします。

Issueに対する開発を行う場合は、実装を始める前に必ず対象Issueを確認してください。

対象Issueから関連Issue・Pull Requestが参照されていて、
実装や設計判断に関係する場合は、それらも確認してください。

特に、過去のIssueやPull Requestに設計判断の理由が記録されている場合は、
その内容を尊重してください。

Issueの内容、現在のコード、既存の設計に矛盾がある場合は、
独断で大きな設計変更をせず、ユーザーに確認してください。

## 開発方針

- 必要以上に複雑な設計や抽象化を行わない
- 現在必要な機能を満たすシンプルな実装を優先する
- 将来必要になるか分からない機能を先回りして実装しない
- 新しいライブラリを追加する場合は、本当に必要か検討する
- 既存コードの設計や命名を確認し、一貫性を保つ
- 関係のないコードをついでに変更しない

## データ設計

`src/data` 以下のJSONをアプリケーションの元データとして扱います。

基本方針は「JSONには事実だけを持つ」です。

- 既存データから導出できる値はJSONに保存しない
- 導出値はTypeScriptで計算する
- 具体的な機能で必要になるまで新しいデータ項目を追加しない
- データ構造を変更する場合は、既存の設計意図をIssueやコードから確認する

研修生のみで活動を終了し、Hello! Projectのグループ・ユニット等で
デビューしていない人物は掲載対象としません。

## TDD

機能開発・バグ修正は原則としてTDDで進めます。

基本的に以下の順序で進めてください。

1. 要件を確認する
2. 失敗するテストを書く
3. テストが意図した理由で失敗することを確認する
4. テストを通す最小限の実装を行う
5. テストが通ることを確認する
6. 必要であればリファクタリングする
7. テスト・lintを実行して最終確認する

テストコードと、そのテストを通すための実装コードは
別コミットに分ける必要はありません。

機能とそのテストを1つの意味のある変更単位として扱います。

## コミットの粒度

1コミットは、1つの意味のある変更単位とします。

各コミットは以下を満たすことを目指します。

- 何を変更したコミットなのか1つの目的で説明できる
- 単独でレビューしやすい
- 原則としてテストが通る状態になっている
- 関係のない変更を含まない

ファイル単位や「テスト」「実装」という理由だけで機械的にコミットを分割しません。

同じ機能を実現するテストと実装は、原則として同じコミットに含めます。

一方で、Issueの実装に複数の独立した変更が必要な場合は、
意味のある単位で複数コミットに分けることを提案してください。

AIエージェント自身はコミットを作成しません。

作業完了時に、変更内容をどのようなコミットに分けるとよいかを判断し、
推奨するコミット構成とコミットメッセージを提示してください。

## ブランチ命名規則

ブランチ名は以下の形式とします。

`prefix/IssueNumber/description-in-kebab-case`

例:

`feat/32/add-delete-button`

`prefix` にはコミットメッセージの `type` と同じ種類を使用します。

使用する主なprefix:

- `feat`: 機能追加
- `bugfix`: バグ修正
- `docs`: ドキュメント更新
- `style`: コードの意味に影響しない変更
- `refactor`: リファクタリング
- `perf`: パフォーマンス改善
- `test`: テストのみの変更
- `chore`: ビルド、補助ツール、ライブラリなどの変更

Issueの内容から適切なprefixとdescriptionを判断してください。

## コミットメッセージ

以下の形式とします。

`type: subject (#IssueNumber)`

例:

`feat: 在籍日数ランキングを表示する (#6)`

type:

- `feat`: 機能追加
- `bugfix`: バグ修正
- `docs`: ドキュメント更新
- `style`: コードの意味に影響しない変更
- `refactor`: リファクタリング
- `perf`: パフォーマンス改善
- `test`: テストのみの変更
- `chore`: ビルド、補助ツール、ライブラリなどの変更

1行目は変更内容を簡潔に日本語で記述します。

1行だけで変更の意図を十分に説明できる場合は、本文を書く必要はありません。

設計上の判断、変更の背景、コードから読み取りにくい理由など、
将来コミットを読む人にとって有用な情報がある場合は、
空行を挟んで本文を記述します。

本文では、単に変更内容を列挙するのではなく、主に以下を説明します。

- 変更前にどのような問題や制約があったか
- なぜこの実装・設計を採用したか
- 重要な代替案を採用しなかった理由（必要な場合）

テストと機能実装を同じコミットに含める場合は `feat`、
テストとバグ修正を同じコミットに含める場合は `bugfix` を使用します。

テストだけを追加・変更する場合に `test` を使用します。

AIエージェント自身はコミットしません。
作業完了時に適切なコミットメッセージを、必要であれば本文も含めて提案してください。

## 検証

コードを変更した場合は、作業完了前に利用可能な検証を実行してください。

基本的に以下を確認します。

- Test: `npm test`
- Lint: `npm run lint`

その他、変更内容に応じて必要な確認があれば実行してください。

検証に失敗した状態で作業を完了しないでください。
解決できない場合は、その内容をユーザーに報告してください。

## 作業依頼

### Issueの開発

以下の形式で依頼された場合:

`issue <ISSUE_NUMBER>`

次の手順で作業してください。

1. `gh` を使って指定されたIssueの内容を確認する
2. Issue内で参照されている関連Issue・Pull Requestのうち、実装に必要なものを確認する
3. 現在のコードとテストを確認する
4. Issue、関連Issue、既存コードから要件を整理する
5. Issueに適した名前で新しいブランチを作成する
6. TDDで実装する
7. テストとlintを実行する
8. `git diff` と `git status` を確認する
9. コミットせずに作業を終了する
10. 実装内容を説明する
11. 推奨するコミットの分け方と、日本語のコミットメッセージを提示する

要件に重要な曖昧さがあり、既存のIssueやコードから判断できない場合は、
勝手に仕様を決めずユーザーに確認してください。

### PR文章の作成

以下の形式で依頼された場合:

`pr`

現在のブランチ名からIssue番号を特定してください。

現在のブランチのコミット、対象Issue、
必要に応じて関連Issue・Pull Requestを確認し、
Pull Requestに使用する文章を提示してください。

PRは作成しないでください。

以下を提示してください。

- PRタイトル
- PR本文

PR本文では、必要に応じて以下を含めてください。

- 概要
- 変更内容
- 動作確認
- 関連Issue

対象IssueをクローズするPRの場合は、
本文に `Closes #<ISSUE_NUMBER>` を含めてください。

GitHub上でPRを自動作成しないでください。
ユーザーが文章をレビューした後、自分でPRを作成します。

## 禁止事項

明示的に依頼されない限り、以下を行わないでください。

- `git commit`
- `git push`
- Pull Requestの作成
- Issueの作成・編集・クローズ
- 既存コミットのamendやrebase
- 大規模な設計変更
- 不要な依存ライブラリの追加
